version: "3.9"

services:
  grobid:
    image: lfoppiano/grobid:0.8.2
    container_name: grobid
    ports:
      - "8070:8070"

  app:
    build: .
    container_name: paper-analysis-app
    depends_on:
      - grobid
    volumes:
      - ./papers:/app/papers
      - ./results:/app/results
